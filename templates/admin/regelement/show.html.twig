{% extends 'Admin/base.html.twig' %}

{% block title %}Regelement{% endblock %}

{% block body %}
<div class="container">
    <h1>Regelement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ regelement.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ regelement.libelle }}</td>
            </tr>
            <tr>
                <th>MontantRegelement</th>
                <td>{{ regelement.montantRegelement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('regelement_index') }}">back to list</a>

    <a href="{{ path('regelement_edit', {'id': regelement.id}) }}">edit</a>

    {{ include('Admin/regelement/_delete_form.html.twig') }}
</div>
{% endblock %}
