{% extends 'admin/base.html.twig' %}

{% block title %}Regelements{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center mt-4 mb-4">Regelements {{ Achat.fournisseur.NomComplet }}</h1>

    <table class="table table-hover small">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ Achat.id }}</td>
            </tr>
            <tr>
                <th>designation</th>
                  <td>{{ Achat.designation }}</td>
            </tr>
            <tr>
                <th>libelle</th>
                  <td>{{ Achat.libelle }}</td>
            </tr>
            <tr>
                <th>fournisseur</th>
                 <td>{{ Achat.fournisseur.NomComplet }}</td>
            </tr>
            <tr>
                <th>Banque</th>
                  <td>{{ Achat.banque }}</td>
            </tr>
             <tr>
                <th>Mode</th>
                  <td>{{ Achat.Mode.modeReg }}</td>
            </tr>
             <tr>
                <th>credit</th>
                  <td>{{ Achat.credit|number_format(2, '.', ',') }}</td>
            </tr>
            <tr>
                <th>observation</th>
                 <td>{{ Achat.observation }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('Regelements_index') }}">Retour</a>

    <a href="{{ path('Regelements_edit', {'id': Achat.id}) }}">edit</a>

    {{ include('admin/Regelements/_delete_form.html.twig') }}
{% endblock %}
