{% extends 'Admin/base.html.twig' %}

{% block title %}ModeRegelement{% endblock %}

{% block body %}
    <h1>ModeRegelement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_regelement.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ mode_regelement.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mode_regelement_index') }}">back to list</a>

    <a href="{{ path('mode_regelement_edit', {'id': mode_regelement.id}) }}">edit</a>

    {{ include('Admin/mode_regelement/_delete_form.html.twig') }}
{% endblock %}
